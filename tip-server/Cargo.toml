[workspace]
resolver = "2"
members = [
    "crates/tip-core",
    "crates/tip-protocol",
    "crates/tip-server",
    "testkit",
]

[workspace.dependencies]
openssl = { version = "0.10", features = ["vendored"] }

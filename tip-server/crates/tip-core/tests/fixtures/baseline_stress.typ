Text before $a$ middle $a^a$ and $a_b$ then $a^(a^a)$ deep $a^(a^(a^a))$ end.

$
    integral f dif x
$

More: $x$ vs $x^2$ vs $x^(2^3)$ vs $x_(i_j)$ vs $x_(i_(j_k))$ here.

Fractions: $frac(1,2)$ inline $frac(a, frac(b, c))$ nested $frac(x^2, y_i)$ mixed.

Big: $sum_(i=0)^n$ vs inline $integral_0^1$ vs $product_(k=1)^n$ here.

Brackets: $lr(angle.l frac(a,b) angle.r)$ and $lr(| x |)$ and $norm(x)$ end.

Mix: text $alpha$ text $alpha^beta$ text $alpha_(beta^gamma)$ text $sqrt(frac(a^2,b_3))$ end.

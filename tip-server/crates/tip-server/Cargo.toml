[package]
name = "tip-server"
version = "0.1.0"
edition = "2021"

[dependencies]
tip-core = { path = "../tip-core" }
tip-protocol = { path = "../tip-protocol" }
serde_json = "1"
openssl = { workspace = true }

[dev-dependencies]
testkit = { path = "../../testkit" }

[package]
name = "tip-server-typst"
version = "0.1.0"
edition = "2021"

[dependencies]
tip-core-typst = { path = "../tip-core-typst" }
tip-protocol = { path = "../tip-protocol" }
serde_json = "1"
openssl = { workspace = true }

[dev-dependencies]
testkit = { path = "../../testkit" }
